import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as CodeStories from "./Code.stories";

<Meta of={CodeStories} />

# Code

Inline code and fenced blocks with optional line highlights.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={CodeStories.Preview} />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={CodeStories.LivePreview} meta={CodeStories} />

<Source of={CodeStories.LivePreview} />

## Source Mode

<Canvas of={CodeStories.SourceMode} meta={CodeStories} />

<Source of={CodeStories.SourceMode} />

## Syntax

```md
Inline `code`
```
