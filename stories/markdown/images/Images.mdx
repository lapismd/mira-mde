import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as ImagesStories from "./Images.stories";

<Meta of={ImagesStories} />

# Images

Images resolve through the configured asset resolver. Data-URI (`base64`) images render without a resolver.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={ImagesStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={ImagesStories.LivePreview} meta={ImagesStories} />

<Source of={ImagesStories.LivePreview} language="html" />

## Source Mode

<Canvas of={ImagesStories.SourceMode} meta={ImagesStories} />

<Source of={ImagesStories.SourceMode} language="html" />

## Syntax

Path image (resolved by `assetResolver` / `fileAdapter`):

```md
![Alt](/mira-markdown-demo.svg "Optional title")
```

Inline base64:

```md
![Alt](data:image/png;base64,...)
```

Reference-style base64 (default when pasting or inserting via the toolbar):

```md
![Alt][label]

[label]: data:image/png;base64,...
```

Paste, drop, and the toolbar image action use `imageConfig` on the editor. See **Default UI → Editor Plugins → Image Attachments**.
