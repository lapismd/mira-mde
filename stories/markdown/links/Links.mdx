import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as LinksStories from "./Links.stories";

<Meta of={LinksStories} />

# Links

Relative, external, and automatic links resolve through the configured adapter.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={LinksStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={LinksStories.LivePreview} meta={LinksStories} />

<Source of={LinksStories.LivePreview} language="html" />

## Source Mode

<Canvas of={LinksStories.SourceMode} meta={LinksStories} />

<Source of={LinksStories.SourceMode} language="html" />

## Syntax

```md
[relative](notes/markdown.md)
```
