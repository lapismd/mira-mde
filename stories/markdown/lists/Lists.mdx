import { Meta, Canvas, Primary, Controls } from "@storybook/addon-docs/blocks";
import * as ListsStories from "./Lists.stories";

<Meta of={ListsStories} />

# Lists

Ordered and unordered lists keep nested alignment and wrapping.

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={ListsStories.LivePreview} meta={ListsStories} />

## Source Mode

<Canvas of={ListsStories.SourceMode} meta={ListsStories} />

## Syntax

```md
- Item
  - Nested
```
