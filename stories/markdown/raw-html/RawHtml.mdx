import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as RawHtmlStories from "./RawHtml.stories";

<Meta of={RawHtmlStories} />

# Raw HTML

Trusted raw HTML is preserved in preview surfaces.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={RawHtmlStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraEditor`).

<Primary />

<Controls />

## Live Preview

<Canvas of={RawHtmlStories.LivePreview} meta={RawHtmlStories} />

<Source of={RawHtmlStories.LivePreview} language="html" />

## Source Mode

<Canvas of={RawHtmlStories.SourceMode} meta={RawHtmlStories} />

<Source of={RawHtmlStories.SourceMode} language="html" />

## Syntax

```md
<mark>highlighted</mark>
```
