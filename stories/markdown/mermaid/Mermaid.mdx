import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as MermaidStories from "./Mermaid.stories";

<Meta of={MermaidStories} />

# Mermaid

Mermaid diagrams render inline with Lapis-compatible controls.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={MermaidStories.Preview} />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={MermaidStories.LivePreview} meta={MermaidStories} />

<Source of={MermaidStories.LivePreview} />

## Source Mode

<Canvas of={MermaidStories.SourceMode} meta={MermaidStories} />

<Source of={MermaidStories.SourceMode} />

## Syntax

````md
```mermaid
flowchart TB
  A --> B
```
````

```

```
