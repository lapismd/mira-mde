import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as MermaidStories from "./Mermaid.stories";

<Meta of={MermaidStories} />

# Mermaid

Mermaid diagrams render inline with Lapis-compatible controls.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={MermaidStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraEditor`).

<Primary />

<Controls />

## Dialog controls

Every pan, zoom, and reset button has a stable accessible name, a hover title,
and native keyboard behavior.

<Canvas of={MermaidStories.DialogControls} meta={MermaidStories} />

## Live Preview

<Canvas of={MermaidStories.LivePreview} meta={MermaidStories} />

<Source of={MermaidStories.LivePreview} language="html" />

## Source Mode

<Canvas of={MermaidStories.SourceMode} meta={MermaidStories} />

<Source of={MermaidStories.SourceMode} language="html" />

## Syntax

````md
```mermaid
flowchart TB
  A --> B
```
````

```

```
