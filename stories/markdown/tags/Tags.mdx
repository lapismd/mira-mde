import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as TagsStories from "./Tags.stories";

<Meta of={TagsStories} />

# Tags

Inline and nested tags render as navigable metadata.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={TagsStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraEditor`).

<Primary />

<Controls />

## Live Preview

<Canvas of={TagsStories.LivePreview} meta={TagsStories} />

<Source of={TagsStories.LivePreview} language="html" />

## Source Mode

<Canvas of={TagsStories.SourceMode} meta={TagsStories} />

<Source of={TagsStories.SourceMode} language="html" />

## Syntax

```md
#mira #mira/docs
```
