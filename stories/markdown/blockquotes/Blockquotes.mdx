import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as BlockquotesStories from "./Blockquotes.stories";

<Meta of={BlockquotesStories} />

# Blockquotes

Blockquotes preserve quoted prose and nesting.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={BlockquotesStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={BlockquotesStories.LivePreview} meta={BlockquotesStories} />

<Source of={BlockquotesStories.LivePreview} language="html" />

## Source Mode

<Canvas of={BlockquotesStories.SourceMode} meta={BlockquotesStories} />

<Source of={BlockquotesStories.SourceMode} language="html" />

## Syntax

```md
> Quoted prose
```
