import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as DirectivesStories from "./Directives.stories";

<Meta of={DirectivesStories} />

# Directives

Directive syntax renders as portable custom elements.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={DirectivesStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraEditor`).

<Primary />

<Controls />

## Live Preview

<Canvas of={DirectivesStories.LivePreview} meta={DirectivesStories} />

<Source of={DirectivesStories.LivePreview} language="html" />

## Source Mode

<Canvas of={DirectivesStories.SourceMode} meta={DirectivesStories} />

<Source of={DirectivesStories.SourceMode} language="html" />

## Syntax

```md
:::mira{title="Example"}
Content
:::
```
