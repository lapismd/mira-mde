import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as InlineFormattingStories from "./InlineFormatting.stories";

<Meta of={InlineFormattingStories} />

# Inline Formatting

Bold, italic, strikethrough, and inline code compose inside paragraphs.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={InlineFormattingStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraEditor`).

<Primary />

<Controls />

## Live Preview

<Canvas
  of={InlineFormattingStories.LivePreview}
  meta={InlineFormattingStories}
/>

## Source Mode

<Canvas
  of={InlineFormattingStories.SourceMode}
  meta={InlineFormattingStories}
/>

## Syntax

```md
Use **bold**, _italic_, and `code`.
```
