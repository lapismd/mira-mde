import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as GridTablesStories from "./GridTables.stories";

<Meta of={GridTablesStories} />

# Grid Tables

Grid tables use explicit boundaries for spans, sections, and alignment.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={GridTablesStories.Preview} />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={GridTablesStories.LivePreview} meta={GridTablesStories} />

<Source of={GridTablesStories.LivePreview} />

## Source Mode

<Canvas of={GridTablesStories.SourceMode} meta={GridTablesStories} />

<Source of={GridTablesStories.SourceMode} />

## Syntax

```md
+---+---+
| A | B |
```
