import {
  Meta,
  Canvas,
  Primary,
  Controls,
  Source,
} from "@storybook/addon-docs/blocks";
import * as MathStories from "./Math.stories";

<Meta of={MathStories} />

# Math

Inline and block KaTeX math rendering.

## Usage

Docs source shows the public Mira APIs (not story wrapper components).

<Source of={MathStories.Preview} language="html" />

## Surfaces

Each markdown feature is shown in reading preview (`MarkdownPreview`), live preview, and source mode (`MiraDefaultMde`).

<Primary />

<Controls />

## Live Preview

<Canvas of={MathStories.LivePreview} meta={MathStories} />

<Source of={MathStories.LivePreview} language="html" />

## Source Mode

<Canvas of={MathStories.SourceMode} meta={MathStories} />

<Source of={MathStories.SourceMode} language="html" />

## Syntax

```md
$a^2 + b^2 = c^2$
```
