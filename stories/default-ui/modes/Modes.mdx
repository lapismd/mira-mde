import {
  Meta,
  Canvas,
  Controls,
  Primary,
  Source,
} from "@storybook/addon-docs/blocks";
import * as ModesStories from "./Modes.stories";

<Meta of={ModesStories} />

# Modes

`MiraDefaultMde` exposes four editor surfaces. Mode availability can be limited with feature flags such as `source-mode`, `live-preview-mode`, `preview-mode`, and `split-mode`.

## Usage

<Source
  language="html"
  code={`<MiraDefaultMde bind:value mode="live-preview" />`}
/>

<Primary />

## Properties

<Controls />

## Examples

### Source

<Canvas of={ModesStories.Source} meta={ModesStories} />

### Preview

<Canvas of={ModesStories.Preview} meta={ModesStories} />

### Split

<Canvas of={ModesStories.Split} meta={ModesStories} />
