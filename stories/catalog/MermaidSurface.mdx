import { Meta } from "@storybook/addon-docs/blocks";
import { SvelteDocsBridge } from "./SvelteDocsBridge";
import CatalogPage from "./CatalogPage.svelte";

<Meta
  title="Mira/Catalog/Mermaid surface"
  parameters={{ layout: "fullscreen" }}
/>

# Mermaid surface

The optional Mermaid plugin uses the same inherited semantic, floating-surface,
and code typography tokens as the rest of Mira.

<SvelteDocsBridge component={CatalogPage} props={{ entryIds: ["mermaid"] }} />
