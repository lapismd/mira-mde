---
title: Math
description: Render inline and block math with KaTeX.
---

import LiveEditorExample from "../../../components/LiveEditorExample.svelte";
import { mathMarkdown } from "../../../data/markdown-examples";

Math renders through KaTeX in preview, live preview, and embedded preview surfaces.

<LiveEditorExample
  client:only="svelte"
  title="Math"
  description="Preview inline and block math."
  initialValue={mathMarkdown}
  height="28rem"
  sourcePath="markdown/math.md"
/>

## Syntax

```md
Inline math: $a^2 + b^2 = c^2$

$$
\int_0^1 x^2 dx = \frac{1}{3}
$$
```

Use single dollar signs for inline math and double dollar signs for block math.
