---
title: Raw HTML
description: Preserve raw HTML through the Markdown preview pipeline.
---

import LiveEditorExample from "../../../components/LiveEditorExample.svelte";
import { rawHtmlMarkdown } from "../../../data/markdown-examples";

Raw HTML is preserved by the Markdown renderer when the host application allows it.

<LiveEditorExample
  client:only="svelte"
  title="Raw HTML"
  description="Preview inline and block HTML inside Markdown."
  initialValue={rawHtmlMarkdown}
  height="30rem"
  sourcePath="markdown/raw-html.md"
/>

## Syntax

```md
<mark>highlighted text</mark>
<kbd>keyboard keys</kbd>
```

Raw HTML should be used deliberately because host applications may apply their own sanitization policy.
