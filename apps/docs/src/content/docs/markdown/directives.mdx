---
title: Directives
description: Render portable directive syntax.
---

import LiveEditorExample from "../../../components/LiveEditorExample.svelte";
import { directivesMarkdown } from "../../../data/markdown-examples";

Directives let Markdown include structured extension blocks without hard-coding application UI.

<LiveEditorExample
  client:only="svelte"
  title="Directives"
  description="Preview a container directive rendered as a portable custom element."
  initialValue={directivesMarkdown}
  height="28rem"
  sourcePath="markdown/directives.md"
/>

## Syntax

```md
:::mira{title="Directive example"}
Directive content.
:::
```

Applications can map directive names to custom rendering behavior.
