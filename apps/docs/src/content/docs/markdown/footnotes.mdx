---
title: Footnotes
description: Render GFM footnotes and backlinks.
---

import LiveEditorExample from "../../../components/LiveEditorExample.svelte";
import { footnotesMarkdown } from "../../../data/markdown-examples";

Footnotes keep supporting details near the content that references them while rendering the note text at the end of the document.

<LiveEditorExample
  client:only="svelte"
  title="Footnotes"
  description="Preview a footnote reference and definition."
  initialValue={footnotesMarkdown}
  height="28rem"
  sourcePath="markdown/footnotes.md"
/>

## Syntax

```md
Text with a footnote.[^note]

[^note]: Footnote content.
```

The preview renderer outputs backlink behavior from the shared Markdown pipeline.
